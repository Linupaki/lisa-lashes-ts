[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npx nest build",
  "echo '--- dist contents ---' && ls -la dist/ || echo 'dist/ does not exist!'"
]

[start]
cmd = "node dist/main"
